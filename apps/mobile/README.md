# Mobile App Placeholder

Initialize Expo here when ready.

Suggested command:

```bash
npx create-expo-app@latest .
```

After initialization, keep game logic out of the app layer and import engine behavior from `packages/engine/`.
